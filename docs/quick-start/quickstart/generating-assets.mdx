---
sidebar_position: 3
---

import Image from '@theme/IdealImage';
import importedFile from './images/import_file.png';
import exportedFile from './images/exported.png';
import bundleImg from './images/bundle.png';

# Generating Zernikalos assets

This guide will walk you through the process of generating compatible assets for the Zernikalos engine.

## Prerequisites

Before starting, ensure you have:
- A 3D model in one of the supported formats
- [Nest App installed](./nest-install) on your system

## Step 1: Prepare Your 3D Model

Your 3D model should be in one of these supported formats:
- glTF 2.0 (Recommended)
- OBJ
- COLLADA (.dae)
- FBX

:::tip
We recommend using glTF 2.0 as it provides the best compatibility and performance with Zernikalos engine.
:::

## Step 2: Import to Nest App

1. Open Nest App
2. Navigate to the import section
3. Select your 3D model file
4. Wait for the import process to complete

<div style={{ display: 'flex', justifyContent: 'center', gap: '20px', marginBottom: '20px' }}>
  <div>
    <Image 
      img={importedFile} 
      alt="Nest App Import Screen" 
    />
    <p style={{ textAlign: 'center' }}>Import Screen</p>
  </div>
  <div>
    <Image 
      img={exportedFile}
      alt="Imported data on editor" 
    />
    <p style={{ textAlign: 'center' }}>Imported data on editor</p>
  </div>
</div>

## Step 3: Generate Bundle

1. In Nest App, locate the "Scene" menu
2. Click on "Bundle Scene" option
3. Select your desired output location
4. Wait for the bundling process to complete

:::note
The bundled scene is the only format compatible to be used with Zernikalos engine.
:::

<Image 
    img={bundleImg}
    style={{width: "50%"}}
/>
