# WebZite docs

- **[API link generation](api-links-generation.md)** — How API URLs and link rewriting work (`apiTools`, static params, processDokkaHtml, navigation tree).
- **[Link check](link-check.md)** — How `pnpm links:check` works (serve, linkinator, timeouts, options).
